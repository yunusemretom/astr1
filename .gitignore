# .env file
.env
# .env yedekleri (API anahtarı içerir — asla depoya girmemeli)
.env.bak.*
.env.production

# Python sanal ortamı (uv ile yeniden üretilir — bkz. requirements.txt)
.venv/

# Python derleme çıktıları
__pycache__/
*.py[cod]

# ROS 2 / colcon build çıktıları
build/
install/
log/

vosk-model-small-tr-0.3/
# Yüz tanıma: kişilerin fotoğrafları (kişisel veri — depoya girmez)
faces/
Persons/
# Çalışma zamanı hafızası (kişisel veri; tohum: astro_memory.seed.json)
ros2_ws/astro_memory.json
ros2_ws/astro_memory.json.bak
*.db
ros2_ws/data/
